export const requesterRoutes = {
  requesterDashboard: '/requester-dashboard',
  requestDetail: '/request-detail/:id',
};

export const supporterRoutes = {
  supporterDashboard: '/supporter-dashboard',
};

export const publicRoutes = {
  plannedFeatures: '/planned-features',
  roleSelection: '/role-selection',
  login: '/login',
  register: '/register',
  main: '/',
};